#ifndef SPELLS_H
#define SPELLS_H

#include <string>

const int SIZE = 10;

void getInfoFromUser(std::string banana[], int apple[]);
double calculateAverage(int apple[]);
int findLowest(int apple[]);
int findHighest(int apple[]);

#endif